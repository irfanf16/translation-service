version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:80"
    depends_on:
      - mysql
      - redis

  mysql:
    image: mysql:8
    environment:
      MYSQL_DATABASE: translation_service
      MYSQL_ROOT_PASSWORD: root

  redis:
    image: redis:alpine
